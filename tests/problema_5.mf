import io

def fun void mdc(int n, int m, int r)
    if (m % n == 0)
        r = n
        return

    if (n % m == 0)
        r = m
        return

    int resto = 0
    if (m > n)
        resto = m % n
        mdc(n, resto, r)
    else
        resto = n % m
        mdc(m, resto, r)

end def

def fun void main()
    int a = 48
    int b = 18
    int resultado

    mdc(a, b, resultado)

    print("MDC de ")
    print(a)
    print(" e ")
    print(b)
    print(" = ")
    print(resultado)
    
end def


