def fun int soma_nat(int n)
    if (n <= 0)
        return 0
    return n + soma_nat(n - 1)
end def

def fun int main()
    int r = soma_nat(10)
    print(r)

    string a = "Olá"
    string b = " Mundo!"
    string c = a + b

    print(c)
    
    return 0
end def
