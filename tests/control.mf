import io

def fun int f(int x)
    if (x < 0)
        return -1
    elif (x == 0)
        return 0
    else
        return 1
end def

def fun void main()
    int i = 0
    while (i < 5)
        if (i % 2 == 0)
            print(i)
        else
            print(i + 100)
        i = i + 1

    int[] arr = [10, 20, 30]
    for (v in arr)
        print(v)


    for (v, idx in arr)
        print(idx)
        print(v)
end def